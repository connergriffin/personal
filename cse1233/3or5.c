#include <stdio.h>
int main (void)

{
    int num;
    int a, b;
    /*  printf ("please enter an integer to check:  ");
    scanf("%d", &num);

    if (num % 3 == 0 || num % 5 == 0)
        printf ("the integer %d is a multiple of either 3 or 5\n", num);
    else
        printf ("the integer %d is not a multiple of either 3 or 5\n", num);    */
    printf ("this prints all the numbers from 0 to 50 that are a multiple of 3 or 5");
    while (b - a <= 50)
    {
        
        if (num%3 == 0 || num%5 == 0)
        printf ("%d", num);
    }

    return 0;
}